## 구현 기능 목록
* 문자열 입력 기능
* 쉼표(,)또는 콜론(:)을 통한 숫자 구분 기능
* 기본 구분자(, :) 외 커스텀 구분자 지정 기능
    * 문자열 앞 부분 "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용
    * e.g. "//;\n1;2;3"과 같이 값을 입력할 경우 커스텀 구분자는 세미콜론(;)이다
* 숫자들의 합 계산 기능
* 잘못된 값 입력 경우 `IllegalArgumentException` 발생 기능